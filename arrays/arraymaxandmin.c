#include <stdio.h>
int main()
{
  int n,max,min,ele;
    scanf("%d",&n);
  for(int i=1;i<=n;i++)
  {
     scanf(%d \n",&ele);
           }
           for(int i=1;i<=n;i++)
           {
             if(ele[i]>max)
             {
               max=ele[i];
             }
             if(le[i]<
