#include <stdio.h>
int main()
{
  int n,max,min,ele;
    scanf("%d",&n);
  for(int i=1;i<=n;i++)
  {
     scanf(%d \n",&ele);
           }
           for(int i=1;i<=n;i++)
           {
             if(ele>max)
             {
               max=ele;
             }
             if(ele<min)
             {
               min=ele;
             }
           }
           printf("%d \n",max,min);
           return 0;
           }
